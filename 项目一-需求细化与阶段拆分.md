# 项目一：需求细化与阶段拆分

> 状态：技术路线与业务规则均已确认，正式 Word PRD V1.1 已生成，Phase 0 完成。
>
> 原始依据：`AI编程工具练习项目-需求说明.md`、`学生成绩处理系统 PRD正式文档（MySQL版）.docx`。

## 1. 当前文件夹检查结果

当前目录只有原始需求、一份 MySQL 版 Word PRD 和需求细化文档，尚未初始化 Git，也没有应用代码、Maven/npm 依赖清单或可执行测试。

现有 PRD 已覆盖学生管理、成绩录入、权重、排名、统计和打印，但存在以下开发阻塞项：

1. `student_score` 没有 `(student_id, subject_id, exam_type_id)` 唯一约束，重复提交会产生重复成绩。
2. “未录入”“零分”“缺考”没有区分，无法确定总评和统计口径。
3. 综合得分直接加权求和，结果可能超出 100 分，且学科缺失时不同学生不可公平比较。
4. 数据库不使用物理外键，但删除规则又依赖关联完整性，容易形成孤立成绩数据。
5. “禁止多人同时编辑”在无登录、无用户身份的系统中无法真正执行。
6. 操作日志写入方式未定义，数据库也没有日志表。
7. 原 9 个阶段包含多个前后端任务，仍不满足“每阶段只做一件事”。

## 2. 已确认技术路线

| 层次 | 推荐选型 | 理由 |
|---|---|---|
| Java 运行时 | Java 17 | 本机已安装 LTS 版本，满足 Spring Boot 4.1.0 的运行与编译基线 |
| 后端 | Spring Boot 4.1.0 + Maven Wrapper / Maven 3.9.x | `pom.xml` 父版本使用 `4.1.0`，适合 REST API 与事务练习 |
| 数据访问 | MyBatis-Plus 3.5.17 Boot 4 Starter + MySQL Connector/J | 使用 Boot 4 专用 starter，保留 Mapper 分层并观察 SQL |
| 数据库迁移 | Spring Boot Flyway Starter + `flyway-mysql` | 用版本化 SQL 管理建表和结构变更，依赖版本由 Boot 父 POM 管理 |
| 参数校验 | Spring Boot Validation Starter + Jakarta Validation | 使用 `jakarta.validation.*`，版本由 Boot 父 POM 管理 |
| 数据库 | MySQL Server 8.0.28 | 本机 `MySql80` 服务已运行，可完成真实关系型数据库练习 |
| 前端 | Vue 3 + Vite + Element Plus | 本机 Node.js/npm 已可用，适合表格、表单、弹窗和分页 |
| 图表 | ECharts | 满足饼图与柱状图统计需求 |
| 后端测试 | JUnit 5 + Boot 4 模块化测试 Starter + MockMvc | 使用 WebMVC、Validation、Actuator、Flyway 对应测试 starter |
| 打印 | 浏览器打印 + `@media print` | 不引入打印服务即可完成 A4 预览和打印 |

项目依赖分别由后端 `pom.xml` 和前端 `package.json` 管理，不使用 `venv` 或 Conda。后端提交 Maven Wrapper，优先使用 `mvnw.cmd` 执行构建。Git 必须忽略 `target/`、`node_modules/`、IDE 配置、日志和本地密钥配置。

### 2.1 Spring Boot 4.1.0 后端依赖基线

| 配置项 | 正式基线 | 版本规则 |
|---|---|---|
| Spring Boot 父 POM | `org.springframework.boot:spring-boot-starter-parent:4.1.0` | PRD 技术版本与 POM 父版本统一写为 Spring Boot 4.1.0 |
| Java | `<java.version>17</java.version>` | 保持 Java 17 |
| Web MVC | `org.springframework.boot:spring-boot-starter-webmvc` | 不写版本；不得使用 Boot 3 时代的 `spring-boot-starter-web` |
| 参数校验 | `org.springframework.boot:spring-boot-starter-validation` | 不写版本；代码使用 `jakarta.validation.*` |
| MyBatis-Plus | `com.baomidou:mybatis-plus-spring-boot4-starter:3.5.17` | 固定使用 Boot 4 专用 starter |
| MySQL 驱动 | `com.mysql:mysql-connector-j` | 不写版本，Boot 4.1.0 BOM 管理为 9.7.0，使用 `runtime` scope |
| Flyway | `org.springframework.boot:spring-boot-starter-flyway` + `org.flywaydb:flyway-mysql` | 均不写版本，Boot 4.1.0 BOM 管理 Flyway 12.4.0 |
| Actuator | `org.springframework.boot:spring-boot-starter-actuator` | 不写版本，用于健康检查 |
| 测试 | `spring-boot-starter-webmvc-test`、`spring-boot-starter-validation-test`、`spring-boot-starter-actuator-test`、`spring-boot-starter-flyway-test` | 均为 `org.springframework.boot`、`test` scope，不写版本 |
| 构建 | Maven Wrapper + Maven 3.9.x | 提交 `mvnw`、`mvnw.cmd` 和 `.mvn/wrapper`，Windows 优先执行 `mvnw.cmd` |

禁止使用 `mybatis-plus-spring-boot3-starter`、`mybatis-plus-boot-starter` 或为 Spring、Flyway、MySQL 驱动手写旧版本。项目创建后在 Phase 2 执行 `mvnw.cmd dependency:tree`、`mvnw.cmd test`，并验证健康检查和 Flyway 迁移，验证通过后锁定依赖树。

## 3. 本期明确范围

### 3.1 做什么

- 单校区、单学期、500 名学生以内。
- 单一教务操作员使用，不开发登录、账号和角色权限。
- 学生资料增删改查、搜索、分页和学籍卡打印。
- 学科、考试类型和权重配置。
- 单条与批量成绩录入、修改和删除。
- 学科总评、综合得分、班级排名。
- 及格率和分数段统计图。
- 成绩清单预览与浏览器打印。
- REST API、错误响应、自动化测试和操作日志。

### 3.2 暂不做什么

- 多学期、历史归档、升学或转班流程。
- Excel 导入导出、PDF 文件导出。
- 缺考、缓考、补考等考试状态；本期只区分“未录入”和有效分数。
- 全校或年级排名。
- 多人协作编辑、消息通知、审批流程。
- 移动端专项适配和生产环境部署。

## 4. 精确业务规则

### 4.1 学生与班级

1. 学号去除首尾空格后必须非空、全局唯一，最长 32 个字符。
2. 姓名和班级必须填写；班级以规范字符串保存，本期不单独开发班级管理模块。
3. 查询支持学号精确/前缀匹配、姓名包含匹配、班级精确筛选。
4. 默认每页 20 条，允许 10、20、50 条，最大 100 条。
5. 学生没有成绩时可直接删除；已有成绩时默认返回冲突错误，只有明确选择“同时删除成绩”后才在同一事务中级联删除。

### 4.2 学科、考试类型与权重

1. 学科名称全局唯一；学科权重必须大于 0，最多四位小数。
2. 考试类型名称全局唯一；考试类型权重范围为 0 到 100，最多两位小数。
3. 所有考试类型权重之和必须精确等于 `100.00%` 后，系统才允许进行总评计算。
4. 考试类型权重对所有学科统一生效，本期不支持“不同学科使用不同考试权重”。
5. 已有成绩引用的学科或考试类型不可删除；未被引用时允许删除。
6. 修改权重后不修改原始成绩，所有总评、综合得分和排名按新权重重新计算。

### 4.3 成绩录入

1. 一条成绩由“学生 + 学科 + 考试类型”唯一确定，重复保存执行更新，不新增第二条记录。
2. 有效分数范围为 `0.00` 至 `100.00`，保留两位小数；空输入表示未录入，不自动保存为 0 分。
3. 批量保存必须使用事务：任意一条数据非法时整批失败，不进行部分写入。
4. 前端提交按钮在请求完成前禁用；后端依靠唯一约束和事务保证重复请求不会生成脏数据。
5. 删除成绩后，该学生相应总评、综合得分、排名和统计立即重新计算。

### 4.4 计算规则

1. 学科总评公式：`Σ(考试成绩 × 考试类型权重 / 100)`。
2. 某学科缺少任何一个权重大于 0 的考试成绩时，该学科总评显示“未完成”，不按 0 分计算。
3. 综合得分公式：`Σ(学科总评 × 学科权重) / Σ(学科权重)`，结果保持在 0 至 100 分。
4. 学生缺少任何一个已配置学科的完整总评时，综合得分显示“未完成”，不进入综合排名。
5. 后端计算统一使用 Java `BigDecimal`，最终结果采用 `RoundingMode.HALF_UP` 四舍五入保留两位小数，禁止使用 `double` 直接计算成绩。

### 4.5 排名与统计

1. 排名必须先选择班级，只在该班级内进行。
2. 支持学号、某次考试成绩、学科总评、综合得分的升降序排序。
3. 成绩排名默认高分在前；同分采用稠密排名，例如 `95、95、90` 对应 `1、1、2`。
4. 未完成相应成绩计算的学生不参与排名，列表末尾显示“未排名”。
5. 及格线为 60 分，`>= 60` 为及格；统计对象为已完成的学科总评。
6. 分数段定义为 `[0,60)`、`[60,80)`、`[80,90)`、`[90,100]`。
7. 未完成总评不进入及格率分母，但页面单独显示“未完成数量”。

### 4.6 打印与异常

1. 学籍卡一次只打印一个学生，字段为学号、姓名、性别、班级和备注。
2. 成绩清单按当前班级筛选生成，包含各学科总评、综合得分和班级排名。
3. 打印页面使用 A4 纵向样式；操作按钮、导航和筛选控件不出现在打印内容中。
4. 无数据时显示空状态，不生成空白打印页。
5. API 统一使用 `200/201/204/400/404/409/500` 状态码，并返回统一错误结构：错误代码、用户提示、字段错误详情和请求标识。
6. 关键写操作记录到应用日志文件；本期不建立可查询的审计日志模块。

## 5. 数据模型修订

在现有四张表基础上保留精简模型，但必须增加数据库级完整性约束：

```text
student 1 ---- n student_score n ---- 1 subject
                         |
                         n
                         |
                         1
                     exam_type
```

必需修订：

- `student.student_no` 唯一且非空；`name`、`class_name` 非空。
- `subject.subject_name` 唯一；`weight_rate > 0`。
- `exam_type.type_name` 唯一；`0 <= rate <= 100`。
- `student_score.score` 不设置默认值，且限制在 0 至 100。
- `student_score` 增加联合唯一约束 `(student_id, subject_id, exam_type_id)`。
- 三个关联字段建立物理外键，默认使用 `ON DELETE RESTRICT`；学生显式级联删除由应用事务完成。
- 各表增加 `created_at`、`updated_at`，用于调试和判断数据是否刷新。

## 6. API 模块拆分

| 模块 | 主要接口范围 | 重点验证 |
|---|---|---|
| 健康检查 | `/api/health` | 应用与数据库连接正常 |
| 学生 | `/api/students` | 唯一学号、搜索、分页、删除冲突 |
| 学科 | `/api/subjects` | 名称唯一、正权重、引用删除保护 |
| 考试类型 | `/api/exam-types` | 名称唯一、权重合计 100% |
| 成绩 | `/api/scores` | 单条/批量保存、范围校验、事务回滚 |
| 总评 | `/api/results` | 未完成状态、十进制精确计算 |
| 排名 | `/api/rankings` | 班级隔离、稠密排名、未排名处理 |
| 统计 | `/api/statistics` | 统计口径、空数据和分数边界 |
| 打印数据 | `/api/reports` | 当前筛选范围与字段完整性 |

## 7. 细化后的开发 Phase

每个 Phase 开始前确认上一个 Phase 已验证并提交。下列提交说明是建议格式，不代替你亲自理解和执行 Git 操作。

| Phase | 只完成这一件事 | 产出 | 验证门槛 |
|---|---|---|---|
| 0 | 确认需求与技术路线 | `PRD.md`、术语表、决策记录 | 计算、删除、统计口径和不做范围均无歧义 |
| 1 | 建立 Git 项目基线 | `.gitignore`、README、前后端目录骨架 | `git status` 干净，基线提交存在 |
| 2 | 建立 Spring Boot 4.1.0 空应用 | Maven Wrapper 项目、Boot 4 依赖、配置分层、健康检查 | `mvnw.cmd dependency:tree` 与 `mvnw.cmd test` 通过，`/api/health` 返回 200 |
| 3 | 建立 Vue 空应用 | Vite 项目、路由、API 代理 | `npm run build` 通过，页面可访问后端健康检查 |
| 4 | 建立数据库迁移 | Flyway 建表 SQL、Mapper 基础配置 | MySQL 表、索引、唯一约束和外键与 PRD 一致 |
| 5 | 实现学生后端 API | 学生 CRUD、查询、分页 | MockMvc 正常和异常接口测试全部通过 |
| 6 | 实现学生管理页面 | 学生列表、表单、搜索、分页 | 页面完整走通学生 CRUD |
| 7 | 实现学籍卡打印 | 单学生打印视图和打印样式 | A4 预览字段完整、无操作控件 |
| 8 | 实现学科后端 API | 学科 CRUD、权重校验、删除保护 | 唯一名称、正权重、引用删除保护通过 |
| 9 | 实现学科配置页面 | 学科列表和编辑表单 | 页面操作与后端规则一致 |
| 10 | 实现考试类型后端 API | 类型 CRUD、总权重校验、删除保护 | 合计不为 100% 时不可启用计算 |
| 11 | 实现考试类型配置页面 | 类型列表、权重编辑和合计提示 | 页面实时显示合计并正确呈现后端错误 |
| 12 | 实现成绩后端 API | 单条/批量保存、修改、删除 | 唯一成绩、范围校验、整批回滚通过 |
| 13 | 实现成绩录入页面 | 班级学生矩阵与批量保存 | 空值与 0 分区分，失败提示可定位 |
| 14 | 实现总评计算服务 | 学科总评、综合得分 | `BigDecimal` 公式、缺失成绩和舍入测试通过 |
| 15 | 实现排名后端 API | 排序、稠密排名、未排名状态 | 固定样例得到 `1、1、2`，班级数据隔离 |
| 16 | 实现排名页面 | 排名维度和升降序交互 | 页面结果与接口及固定测试样例一致 |
| 17 | 实现统计后端 API | 及格率、分数段和未完成计数 | 边界值 59.99/60/79.99/80/89.99/90/100 正确 |
| 18 | 实现统计图页面 | ECharts 饼图和柱状图 | 筛选联动、空数据和接口失败状态正常 |
| 19 | 实现成绩报表后端查询 | 班级成绩清单数据接口 | 字段、排序和筛选范围符合 PRD |
| 20 | 实现成绩打印页面 | 班级成绩清单打印视图 | A4 预览、分页和字段完整性通过 |
| 21 | 完成全流程验收 | 测试记录、演示脚本、运行说明 | 后端测试和前端构建通过，核心业务可从零演示 |

## 8. Phase 0 决策状态

已确认技术栈：Java 17、Spring Boot 4.1.0、Vue 3、Element Plus 和 MySQL Server 8.0.28。

已确认以下业务决定：

1. 考试类型权重对所有学科统一，不做学科独立考试权重。
2. 未录入成绩不等于 0 分；缺少必需成绩时总评为“未完成”。
3. 综合排名只做班级排名，同分采用 `1、1、2` 稠密排名。
4. 本期不实现缺考/补考、多学期、登录权限和 Excel 导入导出。

正式文档已更新：`学生成绩处理系统-正式PRD-V1.1.docx`。V1.0 保留为历史版本。Phase 0 到此结束；开始 Phase 1 前应先确认 Git 项目基线的执行计划，仍不得提前创建业务代码。
